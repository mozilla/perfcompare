version: 2.1
orbs:
    node: circleci/node@5.0.1
jobs:
  test:
    docker:
      - image: cimg/node:12.16
    steps:
        - checkout
        - run:
              name: npm install
              command: npm install
        - run: echo "npm run lint"
        - run: echo "npm run test"
